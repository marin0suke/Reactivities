namespace Application.Interfaces
{
    public interface IUserAccessor
    {
        string GetUsername();
    }
}

// 158. adding infrastructure project. this is new interface inside new folder. will only do 1 thing.